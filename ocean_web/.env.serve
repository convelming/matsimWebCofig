# 页面标题
VITE_APP_TITLE = "matsim_visualization"

# 生产环境配置
ENV = 'serve'

# 项目接口请求路径
VITE_APP_BASE_API = ''

# 项目部署路径
VITE_APP_PUBLIC_PATH = '/vue/'

# 外部静态文件目录
VITE_APP_EXTERNAL_FILE_PATH = '/vue/static/external'

# 打包目录
VITE_APP_OUTPUT_DIR = "../../matsimWebViz/src/main/resources/static/vue"